name: Build

on:
  push:
    branches:
      - master

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      env:
        CI: false
    - name: Use Node.js
      uses: actions/setup-node@v1
    - run: npm install
    - run: npm run build    
    - run: ls



